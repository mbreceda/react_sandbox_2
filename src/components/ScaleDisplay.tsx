const ScaleDisplay = ({ scale }) => (
  <div className="border border-gray-400 rounded-md h-[31px] flex items-center justify-center w-full text-xl">
    {scale} %
  </div>
);

export default ScaleDisplay;
