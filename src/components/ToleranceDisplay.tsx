const ToleranceDisplay = ({ tolerance }) => (
  <div className="border border-gray-400 rounded-md  flex items-center justify-center w-full text-xl">
    {tolerance} mm
  </div>
);

export default ToleranceDisplay;
